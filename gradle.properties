org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true

# Ported to Minecraft 26.2 (from 26.1.2). Minecraft is now unobfuscated and
# Yarn is no longer published for the 26.x line, so we use Mojang's official
# mappings (configured via loom.officialMojangMappings() in build.gradle).
minecraft_version=26.2
loader_version=0.19.3
fabric_api_version=0.158.0+26.2

mod_name=EUClient
mod_id=euclient
mod_version=0.0.1
maven_group=eu.client
